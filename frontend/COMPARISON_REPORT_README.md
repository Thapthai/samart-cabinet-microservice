# รายงานเปรียบเทียบการเบิกอุปกรณ์และการบันทึกใช้กับคนไข้

หน้ารายงานสำหรับเปรียบเทียบจำนวนอุปกรณ์ที่เบิกกับจำนวนที่บันทึกใช้กับคนไข้จริง

## 📋 คุณสมบัติ

### 1. การค้นหาข้อมูล
- ค้นหาด้วย **HN ผู้ป่วย** - ระบบจะดึงรายการเบิกล่าสุดของผู้ป่วย
- ค้นหาด้วย **Usage ID** - ระบุ ID การเบิกโดยตรง
- ปุ่มโหลดข้อมูลพร้อม loading indicator

### 2. แสดงข้อมูลผู้ป่วย
- HN (Hospital Number)
- ชื่อ-นามสกุล
- EN (Episode Number)
- แผนก

### 3. สรุปภาพรวม (Summary Cards)
- **รายการทั้งหมด** - จำนวนอุปกรณ์ทั้งหมดที่เบิก
- **ตรงกัน (Match)** - รายการที่ดำเนินการครบถ้วน
- **ไม่ตรงกัน (Not Match)** - รายการที่ยังต้องนำกลับเข้าตู้

### 4. ตารางเปรียบเทียบ

| คอลัมน์ | คำอธิบาย |
|---------|----------|
| ลำดับ | เลขลำดับรายการ |
| รหัสอุปกรณ์ | order_item_code |
| อุปกรณ์ | order_item_description พร้อม assession_no |
| จำนวนเบิก | จำนวนที่เบิกทั้งหมด (qty) |
| บันทึกใช้กับคนไข้ | จำนวนที่บันทึกใช้แล้ว (qty_used_with_patient) |
| ต้องนำกลับเข้าตู้ | จำนวนที่รอดำเนินการ (qty_pending) + แสดงจำนวนที่คืนแล้ว |
| สถานะ | Match/Not Match |

**สี Background:**
- 🟢 **สีเขียว** - Match (ดำเนินการครบถ้วน)
- 🔴 **สีแดง** - Not Match (ยังมีรายการที่ต้องนำกลับเข้าตู้)

### 5. สถานะ (Status)
- ✅ **Match** - `qty_pending = 0` (ใช้หมด หรือ คืนครบ)
- ❌ **Not Match** - `qty_pending > 0` (ยังมีรายการที่ต้องระบุสถานะ)

### 6. Export ข้อมูล
- Export เป็น CSV file
- ชื่อไฟล์: `comparison_report_{usage_id}_{date}.csv`
- รวมข้อมูลทั้งหมดในตาราง

---

## 🧮 สูตรคำนวณ

```
จำนวนเบิก (qty) = บันทึกใช้กับคนไข้ (qty_used_with_patient) 
                  + คืนเข้าตู้แล้ว (qty_returned_to_cabinet) 
                  + ต้องนำกลับเข้าตู้ (qty_pending)
```

---

## 📍 เข้าถึงหน้านี้

### จาก Sidebar
1. คลิกเมนู **"เวชภัณฑ์"**
2. เลือก **"รายงานเปรียบเทียบ"**

### จาก URL
```
/medical-supplies/comparison
```

---

## 🔌 API ที่ใช้

### 1. ค้นหารายการเบิกด้วย HN
```typescript
GET /medical-supplies?patient_hn={hn}&page=1&limit=1
```

### 2. ดึงข้อมูล Usage
```typescript
GET /medical-supplies/{usageId}
```

### 3. ดึงรายการ Supply Items
```typescript
GET /medical-supply-items/usage/{usageId}
```

---

## 💡 วิธีใช้งาน

### ตัวอย่างที่ 1: ค้นหาด้วย HN
```
1. ใส่ HN ผู้ป่วย เช่น "20-010334"
2. คลิกปุ่ม "ค้นหา"
3. ระบบจะดึงรายการเบิกล่าสุดของผู้ป่วย
4. แสดงข้อมูลผู้ป่วยและตารางเปรียบเทียบ
```

### ตัวอย่างที่ 2: ค้นหาด้วย Usage ID
```
1. ใส่ Usage ID เช่น "456"
2. คลิกปุ่ม "โหลด"
3. แสดงข้อมูลการเบิกนั้นๆ โดยตรง
```

### ตัวอย่างที่ 3: Export ข้อมูล
```
1. หลังจากมีข้อมูลแสดงในตาราง
2. คลิกปุ่ม "Export CSV"
3. ไฟล์จะ download อัตโนมัติ
```

---

## 🎨 Color Coding

### Badge สถานะ
- 🟢 **Match** (สีเขียว) - `bg-green-600`
- 🔴 **Not Match** (สีแดง) - `bg-red-600`

### จำนวน Pills
- 🔵 **จำนวนเบิก** - `bg-blue-100 text-blue-800`
- 🟣 **ใช้กับคนไข้** - `bg-purple-100 text-purple-800`
- 🟠 **ต้องนำกลับ (pending)** - `bg-orange-100 text-orange-800`
- 🔷 **คืนแล้ว** - `bg-cyan-100 text-cyan-800`

### Background แถว
- 🟢 **Match** - `bg-green-50 hover:bg-green-100`
- 🔴 **Not Match** - `bg-red-50 hover:bg-red-100`

---

## 📁 โครงสร้างไฟล์

```
frontend/src/app/medical-supplies/
├── comparison/
│   └── page.tsx                         # หน้ารายงานเปรียบเทียบ
└── components/
    └── ComparisonTable.tsx              # ตารางแสดงข้อมูลเปรียบเทียบ
```

---

## 🔧 Components

### 1. ComparisonTable
**Props:**
- `items: any[]` - รายการ supply items พร้อมข้อมูล quantity

**Features:**
- แสดงตารางพร้อมสีตามสถานะ
- คำนวณ qty_pending อัตโนมัติ
- แสดง badge สถานะ
- แสดงคำอธิบาย (Legend)

### 2. ComparisonReportPage
**Features:**
- Search by HN or Usage ID
- Patient information display
- Summary cards
- Export to CSV
- Loading states
- Error handling

---

## 🎯 Use Cases

### Use Case 1: เบิก 5 ใช้ 3 คืน 0
```
จำนวนเบิก: 5
ใช้กับคนไข้: 3
ต้องนำกลับ: 2
สถานะ: Not Match ❌ (ยังมี 2 ชิ้นที่ต้องระบุสถานะ)
```

### Use Case 2: เบิก 5 ใช้ 5 คืน 0
```
จำนวนเบิก: 5
ใช้กับคนไข้: 5
ต้องนำกลับ: 0
สถานะ: Match ✅ (ใช้หมดแล้ว)
```

### Use Case 3: เบิก 5 ใช้ 3 คืน 2
```
จำนวนเบิก: 5
ใช้กับคนไข้: 3
คืนเข้าตู้แล้ว: 2
ต้องนำกลับ: 0
สถานะ: Match ✅ (ดำเนินการครบแล้ว)
```

---

## 📊 ตัวอย่าง CSV Export

```csv
ลำดับ,รหัสอุปกรณ์,อุปกรณ์,จำนวนเบิก,บันทึกใช้กับคนไข้,ต้องนำกลับเข้าตู้,สถานะ
1,S4214JELCO018,JELCO IV NO 18,10,10,0,Match
2,BD POSIFLUSH SP 10ML,BD POSIFLUSH SP 10ML (0.9%NaCl),15,10,5,Not Match
3,BD POSIFLUSH SP 5ML,BD POSIFLUSH SP 5ML (0.9%NaCl),7,7,0,Match
```

---

## 🚀 เทคโนโลยีที่ใช้

- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Shadcn/ui** Components
- **Lucide Icons**
- **React Hooks**
- **Axios** (API calls)

---

## 📝 หมายเหตุ

1. ระบบจะคำนวณ `qty_pending` อัตโนมัติจาก `qty - qty_used - qty_returned`
2. สถานะ Match จะแสดงเมื่อ `qty_pending = 0`
3. ถ้ามีการคืนอุปกรณ์ จะแสดงจำนวนที่คืนในวงเล็บ
4. การ Export CSV รองรับ UTF-8 with BOM สำหรับภาษาไทย

---

## 🔗 Related Documentation

- [QUANTITY_MANAGEMENT_SYSTEM.md](../../backend/apps/medical-supplies-service/QUANTITY_MANAGEMENT_SYSTEM.md)
- [QUANTITY_MANAGEMENT_API.md](../../backend/apps/gateway-api/QUANTITY_MANAGEMENT_API.md)

---

**Version:** 1.0.0  
**Last Updated:** 2024-12-20
