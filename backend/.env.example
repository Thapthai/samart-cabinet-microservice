# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# =================================
# DATABASE CONFIGURATION
# =================================
DATABASE_URL="mysql://pose_user:userpassword@localhost:3306/pose_microservice"
ARCHIVE_DATABASE_URL="mysql://pose_user:userpassword@localhost:3306/pose_microservice"

# Database Connection (for docker-compose.dev.yml)
DB_HOST=database
DB_PORT=3306
DB_NAME=pose_microservice
DB_USER=pose_user
DB_PASSWORD=userpassword

# =================================
# MICROSERVICES CONFIGURATION
# =================================

# Gateway API Configuration
GATEWAY_PORT=3000

# Auth Service Configuration
AUTH_SERVICE_HOST=auth-service
AUTH_SERVICE_PORT=3001

# Item Service Configuration
ITEM_SERVICE_HOST=item-service
ITEM_SERVICE_PORT=3002

# Email Service Configuration
EMAIL_SERVICE_HOST=email-service
EMAIL_SERVICE_PORT=3003

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# =================================
# APPLICATION CONFIGURATION
# =================================

# Environment
NODE_ENV=production

# Logging
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGIN=http://localhost:3001
FRONTEND_URL=http://localhost:3001

# =================================
# SECURITY CONFIGURATION
# =================================

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-here-change-this-in-production"
JWT_EXPIRES_IN=24h

# Encryption (for item-service)
ENCRYPTION_KEY="dev-encryption-key-32-chars-long!!!"

# Password Hashing
BCRYPT_ROUNDS=10

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# =================================
# OAuth 2.0 PROVIDERS
# =================================

# Firebase
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Key\n-----END PRIVATE KEY-----\n"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3000/auth/oauth2/callback/google"

# Microsoft OAuth
MICROSOFT_CLIENT_ID="your-microsoft-client-id"
MICROSOFT_CLIENT_SECRET="your-microsoft-client-secret"
MICROSOFT_REDIRECT_URI="http://localhost:3000/auth/oauth2/callback/microsoft"

# =================================
# EMAIL SERVICE CONFIGURATION
# =================================

# SMTP Settings
SMTP_HOST="sandbox.smtp.mailtrap.io"
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER="apikey"
SMTP_PASS="your-smtp-password-here"
SMTP_FROM="noreply@yourapp.com"

# Application Settings
APP_NAME="POSE Microservice"
SUPPORT_EMAIL="support@yourapp.com"

# =================================
# SMTP PROVIDER EXAMPLES
# =================================

# Gmail Setup Instructions:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated app password as SMTP_PASS
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_SECURE=true

# Outlook/Hotmail:
# SMTP_HOST="smtp-mail.outlook.com"
# SMTP_PORT=587

# Yahoo:
# SMTP_HOST="smtp.mail.yahoo.com"
# SMTP_PORT=587

# SendGrid:
# SMTP_HOST="smtp.sendgrid.net"
# SMTP_PORT=587
# SMTP_USER="apikey"
# SMTP_PASS="your-sendgrid-api-key"

# Mailgun:
# SMTP_HOST="smtp.mailgun.org"
# SMTP_PORT=587
# SMTP_USER="your-mailgun-username"
# SMTP_PASS="your-mailgun-password"

# Mailtrap (for development/testing):
# SMTP_HOST="sandbox.smtp.mailtrap.io"
# SMTP_PORT=2525
# SMTP_USER="your-mailtrap-username"
# SMTP_PASS="your-mailtrap-password"

# =================================
# NOTES FOR PRODUCTION
# =================================
# 1. Change all default passwords and secrets
# 2. Use strong, randomly generated JWT_SECRET (at least 32 characters)
# 3. Set NODE_ENV=production
# 4. Use appropriate LOG_LEVEL (error or warn)
# 5. Configure proper CORS_ORIGIN for your frontend domain
# 6. Use secure SMTP provider with SSL/TLS
# 7. Keep ENCRYPTION_KEY secure and never commit actual keys to git
